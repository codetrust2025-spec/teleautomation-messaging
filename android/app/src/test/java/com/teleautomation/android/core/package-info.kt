/**
 * JVM unit + property test source set for the `core` pure-logic layer.
 *
 * Property-based tests use kotest-property (min 100 iterations); example/edge-case
 * coverage uses kotest + MockWebServer. Concrete tests are added alongside their
 * production logic in later tasks.
 *
 * This file exists to materialize the unit-test source root in the base layout.
 */
package com.teleautomation.android.core
