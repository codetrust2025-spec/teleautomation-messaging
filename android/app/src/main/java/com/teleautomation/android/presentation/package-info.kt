/**
 * `presentation` — MVVM presentation layer.
 *
 * Holds ViewModels that expose immutable UI state via `StateFlow` and consume
 * events via `SharedFlow`. ViewModels call repositories and delegate pure logic
 * to the `core` package.
 *
 * This file exists to materialize the package directory in the base layout.
 */
package com.teleautomation.android.presentation
