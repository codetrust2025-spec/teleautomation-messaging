/**
 * `ui.config` — Compose screens for runtime app configuration.
 *
 * Currently hosts the `BackendConfigScreen`, which lets the Operator view, edit,
 * and persist the configurable Backend base URL (R23.6). The screen observes
 * immutable state from `BackendConfigViewModel` and contains no business logic.
 */
package com.teleautomation.android.ui.config
