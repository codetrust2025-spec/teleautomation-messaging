/**
 * `ui` — Jetpack Compose layer: screens, the navigation shell, theme, and reusable
 * composables (e.g. TransientStateHost, MobileRecordCard, OfflineBanner, ConfirmDialog).
 *
 * The UI observes immutable state from the presentation layer and renders it; it
 * contains no business logic.
 *
 * This file exists to materialize the package directory in the base layout.
 */
package com.teleautomation.android.ui
